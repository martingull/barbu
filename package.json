{
  "name": "barbu",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:ios-assets": "vite build && rsync -a --delete dist/ src-tauri/gen/apple/assets/",
    "cards:process": "node scripts/process-cards.mjs",
    "prepare:ios": "npm run build:ios-assets",
    "test:e2e": "playwright test",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:ios": "tauri ios dev",
    "tauri:android": "tauri android dev"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tauri-apps/cli": "^2.0.0",
    "sharp": "^0.35.3",
    "typescript": "^5.0.0",
    "vite": "^6.0.0"
  }
}
